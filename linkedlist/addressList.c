#include "addressList.h"

#include <stdlib.h>

void createList(AddressList *addressList){
    *addressList = NULL;
}

void deleteList(AddressList *address){
    AddressList previous = *address;

    free(direccion de memoria);
    free(previous);

    AddressList addList = malloc(sizeof(struct str_AddressList));
}